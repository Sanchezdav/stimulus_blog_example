<%= form_with(model: [@post, Comment.new]) do |form| %>
  <div class="form-group">
    <%= form.rich_text_area :content %>
  </div>

  <div class="form-group">
    <%= form.button 'Leave a reply', class: "btn btn-primary" %>
  </div>
<% end %>
